
@{
    ViewBag.Title = "ShowSateMent";
}
<table class="table table-bordered table-striped">
    <tr>
        <th>Sr No</th>
        <th>PartyName </th>
        <th>Cash</th>

     
        <th>Dates</th>
       
    </tr>

    @{


        int count = 1;
    }

    @for (int i = 0; i < Model.Rows.Count; i++)
    {

    <tr>
        <td> @count</td>
        <td>@Model.Rows[i][6]</td>
        <td>@Model.Rows[i][3]</td> 
        <td>@Model.Rows[i][4]</td>

        @* <td><a href="@Url.Action("EditPackSize","Admin", new { @PackId =@Model.Rows[i][0]})">Edit</a></td>*@
        @*<td><a href="/MyAdmin/Edit/@(Model.Rows[i][0])">Edit</a></td>*@
        @* <td><a href="~/Admin/Delete/@(Model.Rows[i][0])">Delete</a></td>*@
    </tr>
        count++;
    }

</table>
<div class="btnbtn-success">
    @Html.ActionLink("Download Pdf", "CreateStateMent", "Admin")
</div>
