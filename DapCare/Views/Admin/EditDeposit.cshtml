@model DapCare.Models.managerparty

@{
    ViewBag.Title = "EditDeposit";
}

<h2>EditDeposit</h2>

@using (Html.BeginForm("EditDeposit", "Admin", FormMethod.Post,
                             new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>managerparty</h4>
        <hr />


        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @{
            List<SelectListItem> data = ViewBag.getAllEMployee;
        }
        <div class="form-group">
            @Html.LabelFor(model => model.EmployeId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.EmployeId, data)
                @Html.ValidationMessageFor(model => model.EmployeId, "", new { @class = "text-danger" })
            </div>
        </div>

        @{
            List<SelectListItem> dataparty = ViewBag.getAllPartyy;
        }

        <div class="form-group">
            @Html.LabelFor(model => model.EmployeId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.EmployeId, dataparty)
                @Html.ValidationMessageFor(model => model.EmployeId, "", new { @class = "text-danger" })
            </div>
        </div>



        <div class="form-group">
            @Html.LabelFor(model => model.Cash, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Cash, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Cash, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.dates, new { htmlAttributes = new { @class = "datepicker" } })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>

@section  scripts{

    <script type="text/javascript">


        $(function () {
            $(".datepicker").datepicker({
                dateFormat: "yy-mm-dd",
                changeMonth: true,
                changeYear: true,
                minDate: new Date(2021, 0, 1),
                maxDate: new Date(2040, 0, 1),
                showOn: "both"
            });
        });
        $(function () {
            $(".datepicker2").datepicker({
                dateFormat: "yy-mm-dd",
                changeMonth: true,
                changeYear: true,
                minDate: new Date(2021, 0, 1),
                maxDate: new Date(2040, 0, 1),
                showOn: "both"
            });
        });
    </script>
}